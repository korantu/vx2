#idndef __environment_h__
#define __environment_h__

//does all the input recieving
struct eventer {
  enum {
  };
  recieve_input(){
  };
};

//does all the drawing
struct renderer {
  draw_frame();
};

#endif // __environment_h__
